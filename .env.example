# --- GLOBAL CONFIGURATION ---
# Log Level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL="INFO"
# Output Format: text, json
OUTPUT_FORMAT="text"

# --- NETWORK AND API CONFIGURATION ---
# If a script requires a general base URL
GLOBAL_API_ENDPOINT="https://api.example.com/v1"

# --- SCRIPT-SPECIFIC CONFIGURATION (Contextual) ---
# To avoid conflicts, we use prefixes per area or script:

# Scope: Auditing
AUDIT_TARGET_SERVER=""
AUDIT_REPORTS_PATH="./audit/reports"

# Scope: Hardening
HARDENING_POLICY_STRICTNESS="high" # low, medium, high

# --- DYNAMIC/CONTEXTUAL VARIABLES ---
# In Instead of a single BASE_URL, we document that scripts
# can look up variables with their own name:
# Example of a script called 'security-scan.sh':
# SECURITY_SCAN_BASE_URL="https://scanner.local"

# --- DEVELOPER NOTES ---
# 1. Copy this file to .env: cp .env.example .env
# 2. Never commit your local .env file.
# 3. Use 'source .env' in your scripts to load these variables.
